namespace Livt.IO

/**
 * Scheduled RAM requesting block storage without changing the Ram contract.
 * Uses synchronous storage internally; methods complete over multiple cycles.
 * The synthesis tool makes the final physical mapping decision.
 */
component BlockRam<T, CAPACITY: int = 2048> : Ram<T, CAPACITY, MemoryStyle.Block> {}
